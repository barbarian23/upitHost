import { connect } from 'react-redux';
import AirportCarStep3 from './index';

const mapStateToProps = (state) => ({
});

export const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(AirportCarStep3);
